name: Output Information
on: workflow_dispatch
jobs: 
  info: 
    runs-on: ubuntu-latest
    steps: 
      - name: Output Github Context
          run: echo "${{ toJSON(github) }}" #This should not be treated as text. We want to access some metadata by Git Hub. This is an expression. 
